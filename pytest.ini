[pytest]
# 只收集 tests/ 下的测试，避免误把 store/、web/ 等运行时/前端目录里的
# test_*.py 当成测试收集（曾导致 55 个 collection ERROR）。
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
